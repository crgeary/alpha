export enum ParamType {
    Request = "request",
    Response = "response",
    Next = "next",
    Params = "param",
    Query = "query",
    Body = "body",
    Headers = "headers",
    Cookies = "cookies",
}
