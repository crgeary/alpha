{
    "name": "@alpha/common",
    "version": "0.0.0",
    "private": true,
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc --project tsconfig.production.json",
        "check-ts": "tsc --noEmit",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "class-transformer": "0.5.1",
        "class-validator": "0.13.2"
    }
}
