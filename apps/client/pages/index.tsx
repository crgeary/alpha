import type { NextPage } from "next";
import Head from "next/head";

const Index: NextPage = () => {
    return (
        <>
            <Head>
                <title>Studio</title>
            </Head>
        </>
    );
};

export default Index;
